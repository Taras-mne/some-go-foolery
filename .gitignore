# macOS
.DS_Store

# Claude Code worktrees / local agent state
.claude/

# Logs
*.log

# Prebuilt binaries (produced by `go build`)
*.exe
claudy-p2p/dist/

# Identity seeds & TOFU keyring must never be committed (0600, per-host)
**/identity.key
**/known_peers.json
