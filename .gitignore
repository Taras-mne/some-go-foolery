# Compiled binaries
/daemon
/daemon-windows-amd64.exe
/relay
/relay-linux-amd64
*.exe

# macOS
.DS_Store

# Data / secrets
/data/
/shared_files/
/test_shared/
*.json
!go.sum

# claudy-p2p (Phase A)
claudy-p2p/dist/
# identity seeds must never be committed (0600, per-host)
**/identity.key
**/known_peers.json
